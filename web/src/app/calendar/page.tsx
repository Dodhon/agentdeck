import { SchedulerBoard } from "@/components/SchedulerBoard";

export default function CalendarPage() {
  return (
    <SchedulerBoard
      title="Calendar"
      description="All scheduled tasks and cron jobs should be visible here."
    />
  );
}
